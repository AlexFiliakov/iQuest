# Test requirements for Apple Health Analytics Dashboard
# Install with: pip install -r requirements-test.txt

# Include base requirements
-r requirements.txt

# Additional test-specific dependencies
pytest-benchmark>=4.0.0
pytest-timeout>=2.1.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0
pytest-html>=3.1.0
pytest-mpl>=0.16.0
pillow>=9.0.0
memory-profiler>=0.60.0
faker>=18.0.0

# Ensure PyQt6 is installed for UI tests
PyQt6>=6.5.0